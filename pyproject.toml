[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "satlib"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.7"

[tool.pytest.ini_options]
pythonpath = ["src"]
